export enum RoleType {
  ADMIN = 'admin',
  AUTHOR = 'author',
  EDITOR = 'editor',
  TRANSLATOR = 'translator',
}

export enum ArticleStatus {
  PUBLISHED = 'published',
  DRAFT = 'draft',
  UNPUBLISHED = 'unpublished',
  DELETED = 'deleted',
}